// Фирменная палитра «Минстрой».
// Единый источник правды по цветам — отсюда берёт тему Ant Design и любые кастомные стили.

export const palette = {
  /** Основной — заголовки-плашки, кнопки, шапки, акценты */
  primary: "#397BB7",
  /** Акцент — ссылки, наведение, второй уровень акцента */
  accent: "#2F6494",
  /** Графит — тёмные секции, «подвал» */
  graphite: "#3E424D",
  /** Чернильный — заголовки на светлом */
  ink: "#2C2B39",
  /** Основной текст абзацев */
  text: "#333335",
  /** Панель — разделители, чётные строки таблиц */
  panel: "#E1E5EE",
  /** Плашка — теги, чипы, выделение (с текстом accent) */
  chip: "#E6F6FF",
  /** Фон секции — фон блоков, заливка карточек */
  section: "#F6FAFF",
  /** Белый — основной фон */
  white: "#FFFFFF",
} as const;

export type PaletteKey = keyof typeof palette;
